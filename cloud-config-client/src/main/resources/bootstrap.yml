server:
  port: 3355  #配置文件生效顺序为 外部文件==>application==>bootstrap

spring:
  application:
    name: config-client-service-bootstrap
  cloud:
    #config客户端配置
    config:
      label: master #分支名称
      name: config #配置文件名称
      profile: dev #配置文件后缀名称 上述3个综合：读取master分支上config-dev.yml的配置文件。拼起来就是http://localhost:3344/master/config-dev.yml
      uri: http://localhost:3344 #配置中心地址
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka


management:
  endpoints:
    web:
      exposure:
        include: "*"

rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
